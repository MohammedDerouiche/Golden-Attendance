
/**
 * This file is for non-sensitive, static application configuration.
 * For dynamic settings that can be changed by users (like penalties),
 * please use the database and the settings page.
 */

// Example of a config value. You could add things like API keys for non-sensitive, public services.
export const EXAMPLE_CONFIG = 'example_value';
